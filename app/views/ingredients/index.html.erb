<%= render 'shared/side_links' %>
<div class="row">
          <div class="col-sm-3"></div>
          <div class="col-sm-8">
          <% @ingredients.each do |ingredient| %>
          <li><%= ingredient.name %></li>
          <% end %>
          <%= will_paginate %>
          </div>

</div>