  <div class="row">
      <div class="col-sm-3"></div>
      <div class="col-sm-8">
              <h3>
                Recipes
              </h3>
      </div>
    </div>
<div class="row">
          <div class="col-sm-3"></div>
          <div class="col-sm-8">
          <% @recipes.each do |recipe| %>
          <li><%= link_to recipe.name, recipe_path(recipe) %></li>
          <% end %>
          <%= will_paginate %>
          </div>

</div>